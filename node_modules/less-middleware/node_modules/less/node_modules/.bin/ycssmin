#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../ycssmin/bin/cssmin" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../ycssmin/bin/cssmin" "$@"
  ret=$?
fi
exit $ret
